# Exponential reference/smoke note (v0.7.0)

## Status and scope

This note freezes the meaning of the accepted bounded exponential reference/smoke validation step on the current `v0.7.0` line.

It is intentionally narrow. It records what the current exponential-first validation step does and does not establish. It does **not** open the next flare-family technical step.

Accepted current validation checkpoint:
- `6ca2c72` — `Add exponential flare reference smoke`

Accepted current context for that checkpoint:
- `c25ab5e` — `Seed exponential flare contract`
- `0fc40a3` — `Freeze v0.7.0 exponential opening meaning`
- `6ca2c72` — `Add exponential flare reference smoke`

## What this step does establish

The current accepted `v0.7.0` line now carries:
- one bounded shared named-flare contract surface inside the current segmented `1D` kernel discipline
- one concrete flare law only: `exponential`
- one bounded reference/smoke validation surface for that first accepted law

The accepted reference/smoke reading is:
- exponential now has one bounded implementation seed
- exponential now has one bounded reference/smoke validation surface
- that validation surface checks finite outputs
- it checks endpoint/profile observability coherence
- it checks particle/flow identity at sampled boundaries
- it checks bounded refinement behavior across `6 / 12 / 24` segment variants at one bounded frequency
- it strengthens benchmark-ready truth for the first accepted flare law

## What this step does not establish

This step does **not** establish:
- tractrix support
- hyperbolic support
- parabolic support
- Le Cleac'h support
- a second flare law
- an arbitrary free-form flare engine
- a generalized horn-law framework
- broader horn-family maturity
- optimizer or platform implications
- Studio or frontend implications
- public-promotion implications

It also does **not** authorize the next flare-family technical step by implication alone.

## Architecture constraint

The current accepted architectural reading remains:
- bounded named-flare contract
- exponential only
- truthful mapping into the current segmented `1D` kernel surface
- no generalized horn-law expansion engine claim

Later flare-family expansion, if any, requires a separately authorized bounded step.

## Control-plane reading after this smoke step

The meaning of the exponential reference/smoke step is now frozen first.

Current default control-plane reading:
- `v0.7.0` remains open as bounded horn-flare expansion within the current segmented `1D` kernel discipline
- the first accepted law is still `exponential`
- the current line now includes both:
  - one bounded implementation seed
  - one bounded reference/smoke validation surface
- no second flare law is opened by this note
- no generalized flare-family growth is opened by this note

Any later flare-family expansion requires a separately authorized bounded step.
