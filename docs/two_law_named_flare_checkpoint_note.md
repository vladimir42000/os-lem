# Two-law named-flare checkpoint note (v0.7.0)

## Status and scope

This note freezes the meaning of the current accepted two-law named-flare checkpoint on the active `v0.7.0` working line.

It is intentionally narrow. It records what the current two-law checkpoint does and does not establish before any decision is made about broader flare-family growth.

Accepted current checkpoint:
- `f39d5f7` — `Add two-law named flare contract regression`

Accepted current context for that checkpoint:
- `c25ab5e` — `Seed exponential flare contract`
- `0fc40a3` — `Freeze v0.7.0 exponential opening meaning`
- `6ca2c72` — `Add exponential flare reference smoke`
- `e879d44` — `Freeze exponential reference smoke meaning`
- `a96fab4` — `Add exponential flare Akabak reference smoke`
- `8ec37bc` — `Seed tractrix flare contract`
- `081fb8b` — `Add tractrix flare reference smoke`
- `8641841` — `Add tractrix flare Akabak reference smoke`
- `f39d5f7` — `Add two-law named flare contract regression`

## What this checkpoint does establish

The current accepted `v0.7.0` line now carries exactly two accepted named flare laws inside the current segmented `1D` kernel discipline:
- `exponential`
- `tractrix`

For each accepted law, the current line now includes:
- one bounded implementation seed
- one bounded internal reference/smoke validation surface
- one bounded Akabak-style external reference/smoke surface

The current line also includes one bounded shared regression-hardening surface for the two-law contract.

The accepted two-law reading is therefore:
- the shared named-flare contract currently hosts exactly two accepted laws
- both laws coexist under the shared parser / waveguide / solve / observability surface
- the current two-law contract remains bounded, truthful, and stable under the segmented `1D` kernel discipline
- the current line does **not** require arbitrary flare machinery to support the accepted two-law platform

## What this checkpoint does not establish

This checkpoint does **not** establish:
- a third flare law of any kind
- hyperbolic support
- parabolic support
- Le Cleac'h support
- an arbitrary free-form flare engine
- a generalized horn-law framework
- generalized horn-family maturity
- optimizer or platform implications
- Studio or frontend implications
- public-promotion implications
- automatic broadening of `v0.7.0` by momentum

It also does **not** authorize any next flare-family technical step by implication alone.

## Architecture constraint

The current accepted architectural reading remains:
- bounded named-flare contract
- exactly two accepted laws: `exponential` and `tractrix`
- truthful mapping into the current segmented `1D` kernel surface
- no generalized horn-law expansion engine claim
- no arbitrary flare-family growth by default

Any later growth beyond the current two-law platform requires a separately authorized bounded step.

## Control-plane reading after this checkpoint

The meaning of the current two-law checkpoint is now frozen first.

Current default control-plane reading:
- `v0.7.0` remains open as bounded horn-flare expansion within the current segmented `1D` kernel discipline
- the accepted named-flare platform currently contains exactly two laws: `exponential` and `tractrix`
- both accepted laws now have:
  - one implementation seed
  - one internal reference/smoke surface
  - one Akabak-style external reference/smoke surface
- the shared two-law contract now has one bounded regression-hardening surface
- no third-law seed is opened by this note
- no generalized horn-law framework is opened by this note

Any later broadening requires separate authorization.
